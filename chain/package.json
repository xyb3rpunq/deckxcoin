{
  "name": "deckxcoin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DeckxCoin (DECKX) — a full node: Bitcoin UTXO+PoW base layer fused with an Ethereum-style contract VM, a Lightning-style payment channel network, SQLite persistence, reorg handling, and a P2P protocol.",
  "license": "MIT",
  "engines": {
    "node": ">=22.18.0"
  },
  "//engines": "22.18 for native TypeScript type stripping; node:sqlite for persistence ships with Node itself, so the node adds no dependencies.",
  "bin": {
    "deckx": "./src/cli.ts",
    "deckxd": "./src/deckxd.ts"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "cli": "node src/cli.ts",
    "genesis": "node src/cli.ts genesis",
    "node": "node src/deckxd.ts",
    "testnet": "node scripts/testnet.ts",
    "export": "node scripts/export-web-data.ts"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^1.7.1",
    "@scure/base": "^1.2.4"
  }
}
